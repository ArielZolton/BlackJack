## How to Run Docker Compose
this is to help you run everything needed for this project without installing anything more than just docker

```bash
docker compose up --build -d
```

```bash
docker compose logs -f api
```

```bash
docker compose down
```