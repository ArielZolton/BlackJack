import './App.css'
import UserCard from './Components/UserCard'
import Dashboard from './Pages/Dashboard'

function App() {
  return (
    <div>
        <Dashboard />
    </div>
  )
}

// create a nice UserCard component, 
// have component dynamically render the data passed in as props.
// Use MUI if you want and have the time.

export default App
